# Logo assets

Each `*.txt` file contains one logo in UTF-8 text. The filename (without `.txt`) is the value accepted by `--logo`.

Add a new logo by creating a file here; `--list-logos` discovers it automatically. `default.txt` is used when the detected system or requested logo is not available and is intentionally omitted from the public list.
